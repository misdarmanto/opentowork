name: source-verification
instructions: |
  Before citing any source, confirm it actually supports the specific
  claim you're attaching it to — never attribute a claim to a source you
  have not read. If a claim can't be backed by a real source, say so
  explicitly instead of presenting it as confirmed.
